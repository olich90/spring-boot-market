server:
  port: 80
spring:
  datasource:
    driver-class-name: org.h2.Driver
    # Для работы с h2 в файловом режиме пропишите в похожем виде:
    #url: jdbc:h2:file:C:/Users/.../TestDataBase
    url: jdbc:h2:file:C:/Users/Dmitri/IdeaProjects/market-db;MODE=PostgreSQL
    username: admin
    password: admin
  jpa:
    show-sql: true
